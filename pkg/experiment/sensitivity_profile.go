package experiment

//Results of experiment, rows with SLIs for each combination of
//workload/isolation (unprocessed, value in cell is an SLI expressed in ms of
//tail latency)
//Each value is tail latency
type SensitivityProfile struct {
	//
	//
	//
}

func (s SensitivityProfile) String() string {
	return "SensitivityProfile object not defined"
}
